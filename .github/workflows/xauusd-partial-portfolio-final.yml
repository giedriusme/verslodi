name: XAUUSD partial portfolio final
on:
  push:
    branches: [xauusd-backtest-20260924]
    paths:
      - 'xauusd_partial_portfolio_final.py'
      - '.github/workflows/xauusd-partial-portfolio-final.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: xauusd-backtest-20260924
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: python -m pip install --upgrade pip pandas numpy requests
      - name: Patch pandas mod-column access
        run: sed -i 's/g\.mod/g["mod"]/g' xauusd_portfolio_discovery.py
      - name: Run final comparison
        run: python xauusd_partial_portfolio_final.py
      - name: Commit results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add partial_portfolio_final_results
          git commit -m "Add final XAUUSD partial portfolio comparison" || exit 0
          git push origin HEAD:xauusd-backtest-20260924
